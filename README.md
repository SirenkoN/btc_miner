## Bitcoin‑майнер (версия 0.1)

> Простейший solo‑майнер на Python, использующий только CPU. Он не предназначен для реального майнинга и может работать медленно даже на современном ПК. Проект пишется исключительно в учебных целях.

---

### 📦 Что есть

- **Python 3.x**  
  Скрипт полностью реализован на стандартной библиотеке + `requests`.

- **Solo‑майнинг**  
  Получает шаблон блока через RPC от локальной ноды, перебирает nonce и отправляет найденный (ха-ха) блок.

- **Поддержка SegWit**  
  Запрашивает шаблон с правилом `segwit`.

---

### 🚀 Как запустить

1. **Клонируйте репозиторий**


2. **Установите зависимости** (если у вас нет `requests`)

   ```bash
   pip install requests
   ```

3. **Настройте RPC‑параметры**

   Откройте файл `miner.py` и заполните:

   ```python
   RPC_USER     = "your_rpc_user"
   RPC_PASSWORD = "your_rpc_password"
   RPC_HOST     = "127.0.0.1"  # адрес ноды
   RPC_PORT     = 8332        # порт JSON‑RPC
   ```

4. **Запустите скрипт**

   ```bash
   python3 miner.py
   ```

---

### 🔧 Как работает

| Шаг | Описание |
|-----|----------|
| 1 | `getblocktemplate` получает данные о следующем блоке (версии, предыдущий хеш, транзакции и т.д.). |
| 2 | Функция `mine_block` перебирает все возможные nonce от 0 до `2**32-1`, вычисляя двойной SHA‑256 хеш заголовка. Если он меньше целевого значения (`target_from_bits`), найдено решение. |
| 3 | Полученный блок отправляется через RPC методом `submitblock`. |

---

### ⚙️ Конфигурация

Все параметры находятся в начале скрипта:

```python
RPC_USER     = "..."
RPC_PASSWORD = "..."
RPC_HOST     = "127.0.0.1"
RPC_PORT     = 8332
```

- **`RPC_USER` / `RPC_PASSWORD`** – имя пользователя и пароль RPC вашей ноды.  
- **`RPC_HOST`** – IP‑адрес (можно использовать `localhost`).  
- **`RPC_PORT`** – порт, открытый для JSON‑RPC. По умолчанию 8332.

---

### 📈 Что можно улучшить

1. Конфигурационный файл — добавить внешнюю конфигурацию.  
2. Таймеры и статистика — расчет и вывод скорости работы майнера.  
3. Информативность — отображение текущей задачи, смены задачи.  
4. Параллельный майнинг — добавление многопоточности.  
5. Поддержка GPU — расчёты на видеокарте.  
6. Полноценная обработка шаблона — обработка всех транзакций из mempool'а.  
7. Обновление RPC‑интерфейса — использовать `bitcoinrpc`.

---

### 🛠️ Требования

- Python 3.6+  
- `requests`

---