## Bitcoin-майнер (версия 0.3)

> Простейший solo-майнер на Python, использующий только CPU. Он не предназначен для реального майнинга и может работать
> медленно даже на современном ПК. Проект пишется исключительно в учебных целях.

---

### 📦 Что есть

- **Solo-майнинг**  
  Получает шаблон блока через RPC, перебирает nonce и отправляет найденный ( :)) блок.

- **Автообновление задания**
  Обнаруживает появление нового блока в сети, перезапускает майнер с новым шаблоном.

---

### 📋 История версий

#### Версия 0.3 (текущая)

- **Автообновление задания**  
  Реализовано автоматическое обнаружение новых блоков в сети и обновление задания майнинга.

- **Корректная проверка условий майнинга**  
  Исправлено фундаментальное сравнение хеша с целевым значением: теперь используется
  `int.from_bytes(hash_val, 'little') < target_int` вместо некорректной реализации с `[:: -1]` и `'big'`.

- **Улучшенная обработка времени блока**  
  Время блока теперь корректно обрабатывается с учётом правил Bitcoin о допустимом отклонении (±2 часа от системного
  времени).

- **Эффективное кэширование шаблона**  
  Шаблон блока кэшируется и используется до тех пор, пока он не станет устаревшим, что снижает нагрузку на ноду.

- **Проверка высоты блока**  
  Добавлена проверка высоты блока для надёжного обнаружения обновлений в цепочке.

- **Улучшенная обработка формата bits**  
  Переписана функция `target_from_bits` для корректной обработки всех случаев компактного представления целевого хеша
  согласно Bitcoin-протоколу.

#### Версия 0.2

- **Разделение на модули**  
  Исходный монолитный файл разделён на отдельные модули по принципу Single Responsibility (SRP):
    - `config.py` — конфигурация
    - `rpc_client.py` — взаимодействие с RPC-нодой
    - `utils.py` — утилиты хеширования и работы с целевыми значениями
    - `block_builder.py` — сборка заголовка блока
    - `main.py` — основная логика и точка входа

#### Версия 0.1

- **Базовая реализация**  
  Первоначальная версия майнера с минимальной функциональностью:
    - Получение шаблона блока через RPC
    - Перебор nonce
    - Отправка найденного блока

---

### 🚀 Как запустить

1. **Клонируйте репозиторий**

   ```bash
   git clone <URL>
   cd bitcoin-miner
   ```

2. **Установите зависимости** (если у вас нет `requests`)

   ```bash
   pip install requests
   ```

3. **Настройте RPC-параметры**

   Откройте файл `config.py` и заполните:

   ```python
   RPC_USER = "your_rpc_user"
   RPC_PASSWORD = "your_rpc_password"
   RPC_HOST = "127.0.0.1"  # Адрес ноды
   RPC_PORT = 8332  # Порт JSON-RPC
   ```

4. **Запустите скрипт**

   ```bash
   python3 main.py
   ```

---

### 🔧 Как работает

| Шаг | Описание                                                                                                                                                                               |
|-----|----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| 1   | `getblocktemplate` получает данные о следующем блоке (версия, предыдущий хеш, транзакции и т.д.).                                                                                      |
| 2   | Функция `mine_block` перебирает все возможные nonce от 0 до `2**32-1`, вычисляя двойной SHA-256 хеш заголовка. Если он меньше целевого значения (`target_from_bits`), найдено решение. |
| 3   | Полученный блок отправляется через RPC методом `submitblock`.                                                                                                                          |

---

### ⚙️ Конфигурация

Все параметры находятся в файле `config.py`:

```python
RPC_USER = "your_rpc_user"
RPC_PASSWORD = "your_rpc_password"
RPC_HOST = "127.0.0.1"
RPC_PORT = 8332
```

- **`RPC_USER` / `RPC_PASSWORD`** — имя пользователя и пароль RPC вашей ноды.
- **`RPC_HOST`** — IP-адрес (можно использовать `localhost`).
- **`RPC_PORT`** — порт, открытый для JSON-RPC. По умолчанию 8332.

---

### 📈 Что можно улучшить

1. ~~Конфигурационный файл — добавить внешнюю конфигурацию.~~ (реализовано в версии 0.2)
2. Таймеры и статистика — вывод количества попыток/сек.
3. ~~Информативность — отображение текущей задачи, смены задачи.~~ (реализовано в версии 0.3)
4. Параллельный майнинг — многопоточность / мультипроцессинг.
5. Поддержка GPU — расчёты на видеокарте.
6. Полноценная обработка шаблона — все транзакции из mempool'а.
7. Обновление RPC-интерфейса — использовать `bitcoinrpc`.

---

### 🛠️ Требования

- Python 3.6+
- `requests`